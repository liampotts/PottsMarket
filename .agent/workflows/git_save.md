---
description: Automatically stage, commit, and push changes to git
---

// turbo-all

1. Add all changes
git add .

2. Commit changes. You should generate a concise and descriptive commit message based on the changes you are saving.
git commit -m "<message>"

3. Push changes
git push
